# Neon Postgres in production. Leave unset locally to use PGLite.
DATABASE_URL=

# Protects POST/GET /api/cron/sync. Vercel cron and your own curl both send:
#   Authorization: Bearer $CRON_SECRET
CRON_SECRET=
